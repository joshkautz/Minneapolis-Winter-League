{
	"name": "minneapolis-winter-league",
	"version": "3.0.0",
	"private": true,
	"type": "module",
	"workspaces": [
		"App",
		"Functions",
		"Shared"
	],
	"scripts": {
		"dev": "npm run build && npm run emulators:start",
		"build": "npm run build --workspace=App && npm run build --workspace=Functions && npm run build --workspace=Shared",
		"format:check": "npm run format:check --workspace=App && npm run format:check --workspace=Functions && npm run format:check --workspace=Shared",
		"format:fix": "npm run format:fix --workspace=App && npm run format:fix --workspace=Functions && npm run format:fix --workspace=Shared",
		"lint:check": "npm run lint:check --workspace=App && npm run lint:check --workspace=Functions && npm run lint:check --workspace=Shared",
		"lint:fix": "npm run lint:fix --workspace=App && npm run lint:fix --workspace=Functions && npm run lint:fix --workspace=Shared",
		"test": "npm run test --workspace=App && npm run test --workspace=Functions && npm run test --workspace=Shared",
		"emulators:start": "firebase emulators:start --import ./.emulator --export-on-exit ./.emulator",
		"emulators:export": "firebase emulators:export ./.emulator",
		"emulators:clear": "rm -rf ./.emulator && mkdir -p ./.emulator",
		"deploy": "firebase deploy",
		"deploy:hosting": "firebase deploy --only hosting",
		"deploy:functions": "firebase deploy --only functions",
		"deploy:firestore": "firebase deploy --only firestore"
	},
	"devDependencies": {
		"firebase-tools": "^14.14.0",
		"typescript": "^5.9.2"
	}
}
